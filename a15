# print file size and name

ls -l $* | awk '
BEGIN {
	print "BYTES", "\t", "FILE"
}

{
	#print $5, "\t", $9
	print $5, "\t", substr($0, index($0,$9))
}

{
	sum += $5
	++filenum
}
END {
	print "TOTAl : ", sum, "bytes (" filenum " files)"
}
'